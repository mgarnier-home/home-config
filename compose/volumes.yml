x-default_opts: &default_opts
  type: cifs
  # device: //192.168.0.254/SSD1/docker-data
  o: "rw,username=${SMB_USER},password=${SMB_PWD},iocharset=utf8,uid=1000,sec=ntlmv2,file_mode=0777,dir_mode=0777"

volumes:
  adguard_data:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/adguard/work
  adguard_conf:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/adguard/conf
  traefik_data:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/traefik
  dashy_config:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/dashy
  dashboard_config:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/dashboard
  nextcloud_db:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/nextcloud/db
  nextcloud_config:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/nextcloud/config
  node_proxy_config:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/node-proxy
  paperless_db:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/paperless/db
  paperless_data:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/paperless/data
  home_assistant_config:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/home-assistant/config
  ntfy_cache:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/ntfy/cache
  ntfy_data:
    driver: local
    driver_opts:
      <<: *default_opts
      device: ${DOCKER_DATA}/ntfy/data
