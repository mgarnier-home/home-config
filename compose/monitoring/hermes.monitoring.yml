version: "3.8"

services:
  glances:
    extends:
      file: monitoring.yml
      service: glances

  docker-socket-proxy:
    extends:
      file: monitoring.yml
      service: docker-socket-proxy

  watchtower:
    extends:
      file: monitoring.yml
      service: watchtower