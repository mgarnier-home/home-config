version: "3.8"

services:
  glances:
    extends:
      file: monitoring.yml
      service: glances
    ports:
      - ${ARTEMIS_GLANCES_PORT}:61208

  docker-socket-proxy:
    extends:
      file: monitoring.yml
      service: docker-socket-proxy
    ports:
      - ${ARTEMIS_SOCKET_PROXY_PORT}:2375

  watchtower:
    extends:
      file: monitoring.yml
      service: watchtower