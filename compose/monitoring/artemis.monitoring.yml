version: "3.8"

services:
  glances:
    extends:
      file: monitoring.yml
      service: glances
    ports:
      - 4200:61208

  docker-socket-proxy:
    extends:
      file: monitoring.yml
      service: docker-socket-proxy
    ports:
      - 4201:2375

  watchtower:
    extends:
      file: monitoring.yml
      service: watchtower